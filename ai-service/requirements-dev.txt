# accessible-investor / AI 파트 — 재학습 · 재측정 · 연구용
#
# 서비스만 돌릴 거면 이 파일은 필요 없다. requirements.txt 만으로 된다.
# 여기 있는 것들은 셋 중 하나를 할 때만 쓴다.
#   (1) 모델을 다시 학습한다        — cli.py train
#   (2) results/ 의 표와 그림을 다시 만든다 — cli.py results
#   (3) 검사를 돌린다               — pytest tests
#
# 설치: pip install -r requirements-dev.txt   (requirements.txt 도 함께 깔린다)

-r requirements.txt

# --- 검사 ---
# 검사 파일은 pytest 없이도 그냥 실행된다 (python tests/test_serving.py).
# pytest 는 한 번에 모아 돌릴 때 편해서 넣어 둔다.
pytest>=8.0

# --- 표 · 그림 재생성 ---
matplotlib>=3.9
seaborn>=0.13
tqdm>=4.66

# --- 이상탐지 후보 비교 ---
# 배포되는 서비스 경로(SV.anomaly)는 robust z + 학습 랭커만 쓴다. 아래는
# 어느 방법을 채택할지 견주어 볼 때 쓴 것들이다.
stumpy>=1.13          # Matrix Profile (numba JIT: 첫 호출 약 30초)
pyod>=2.0             # ECOD, LOF
ruptures>=1.1         # Change Point (반드시 KernelCPD 사용, Pelt(rbf)는 290배 느림)
statsmodels>=0.14     # 공적분 검정
dtaidistance>=2.3     # DTW

# --- 국내 종목 메타 보조 ---
# pykrx 의 전종목 리스트 조회는 2026-07 기준 KRX 엔드포인트 변경으로
# 로그인 없이 실패한다. 1차 소스는 finance-datareader 다.
pykrx>=1.2.8

# --- 뉴스 감성 모델 재학습 (KR-FinBert) ---
# 배포된 models/news_transfer.pkl 을 쓰는 서비스 경로에는 필요 없다.
# 기사 감성을 다시 채점할 때만 쓴다.
torch>=2.4            # CPU 빌드로 충분
transformers>=4.40    # KR-FinBert-SC

# 오디오(soundfile·librosa)와 노트북(jupyter 계열)은 없다. 소리 변환은
# 다른 담당이 맡아 이 저장소에 없고, 노트북은 결과 리포트로 대체했다 —
# 실행 경로가 하나여야 어긋나지 않는다.
