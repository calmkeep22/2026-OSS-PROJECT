"""
accessible-investor — 저시력자·시각장애인용 국내주식 정보 탐색 엔진

초기 노트북 실험(01~06)에서 확정한 로직의 정식 구현체.
노트북은 "무엇이 맞는지"를 정했고, 이 패키지는 "실제로 쓰이는 코드"다.
(노트북 자체는 배포물에서 뺐다 — 결과는 `python cli.py report` 가
 `outputs/report.html` 한 장으로 만든다. 이유는 report.py 주석 참조.)

노트북 대비 달라진 점 (단타 대응)
--------------------------------
노트북 단계의 이상 탐지는 **일봉 60일 기준**이었다. 이건 증권사 급등락 알림의
일반 기준이고, 실측 결과 종목당 약 5주에 1건이 나온다 — 스윙 투자자용 주기다.
단타 사용자에게는 사실상 아무 알림도 없는 것과 같다.

그래서 시간축을 **3개로 분리**했다. 자세한 근거는 anomaly/README 주석 참조.

    T0  실시간   1분봉    즉시 반응        (단타)
    T1  장중     5분봉    진입/이탈 판단   (단타)  ← 기본
    T2  스윙     일봉     보유 종목 점검   (기존 60일 기준 유지)

시간축을 짧게 하면 오경보가 폭증한다. 그걸 막는 세 장치가 이 패키지의 핵심이다.
    1. 시간대 정규화   — 개장 30분 변동성이 장중의 2.4배다 (01에서 측정)
    2. 알림 예산 임계값 — 고정 |z|>3 대신, 원하는 알림 빈도의 분위수를 임계값으로
    3. 확인 단계       — 다음 봉 지속 + 거래량 동반 여부로 신뢰도 등급을 나눔
"""

from __future__ import annotations

__version__ = "0.2.0"

from . import config  # noqa: F401

__all__ = ["config", "__version__"]
