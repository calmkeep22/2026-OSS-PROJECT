# accessible-investor / AI 파트 — 서비스 실행에 필요한 것만
#
# 팀원(백엔드)이 설치할 파일은 이것 하나면 된다. 예측·이상감지·유사도
# 세 기능이 아래 패키지만으로 전부 돈다 — 빈 가상환경에 이 파일만 깔고
# 실측해서 확인했다.
#
# 재학습·재측정·그림 재생성·검사까지 하려면 requirements-dev.txt 를 더 깐다.
# 예전에는 그쪽 것들이 여기 섞여 있어서, 서비스만 쓸 사람도 torch(수백MB)를
# 받아야 했다. 정작 그 딥러닝 모델은 채택되지 않아 배포에서 빠졌다.
#
# Python 3.12 기준. 설치: pip install -r requirements.txt
# pandas 3.0 · numpy 2.5 · scikit-learn 1.9 에서도 동작을 확인했다.

# --- 수치 계산 · 저장된 모델 로드 ---
numpy>=2.0
pandas>=2.2
scipy>=1.14
scikit-learn>=1.5     # models/*.pkl 을 여는 데 필요하다
joblib>=1.4           # 그 pkl 의 직렬화 형식
pyarrow>=17.0         # models/*.parquet (레지스트리 · 비교군 패널)

# --- 시세 조회 (폴백 경로) ---
# 키움 API 봉을 bars= 로 넘겨 주면 쓰이지 않는다. 봉을 넘기지 않았을 때
# 직접 받아 오는 SV.panel() 에서만 쓴다.
finance-datareader>=0.9.96
yfinance>=1.5

# --- 뉴스 수집 (선택) ---
# 없거나 못 받아도 예측은 나온다. 뉴스 피처 4개가 결측이 되고 응답의
# `뉴스반영일수` 가 0 으로 실려 나갈 뿐이다.
requests>=2.32
