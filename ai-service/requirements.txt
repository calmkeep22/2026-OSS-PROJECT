# accessible-investor / AI 파트 실험 환경
# Python 3.12 기준. 설치: pip install -r requirements.txt

# --- 기본 ---
numpy>=2.0
pandas>=2.2
scipy>=1.14
scikit-learn>=1.5
matplotlib>=3.9
seaborn>=0.13
pyarrow>=17.0
tqdm>=4.66
joblib>=1.4           # 파운데이션 모델 직렬화 (models/pooled_*.pkl)

# --- 국내 주식 데이터 ---
# pykrx의 전종목 리스트 조회는 2026-07 기준 KRX 엔드포인트 변경으로
# 로그인 없이 실패한다. finance-datareader를 1차 소스로 쓴다.
finance-datareader>=0.9.96
pykrx>=1.2.8
yfinance>=1.5

# --- 이상 탐지 ---
stumpy>=1.13          # Matrix Profile (numba JIT: 첫 호출 약 30초)
pyod>=2.0             # ECOD, LOF
ruptures>=1.1         # Change Point (반드시 KernelCPD 사용, Pelt(rbf)는 290배 느림)
statsmodels>=0.14     # 공적분 검정
dtaidistance>=2.3     # DTW

# --- 딥러닝 / 감성 분석 ---
torch>=2.4            # CPU 빌드로 충분
transformers>=4.40    # KR-FinBert-SC
onnx>=1.16            # 2단 탐지기 → ONNX (Java에서 파이썬 없이 로드)
onnxruntime>=1.18     # ONNX 추론. 없으면 파이썬 경로로 자동 폴백된다

# 오디오(soundfile·librosa)와 노트북(jupyter 계열)은 뺐다.
# 소리 변환은 다른 담당이 맡아 이 저장소에 없고, 노트북은 결과 리포트
# (results/index.html)로 대체했다 — 실행 경로가 하나여야 어긋나지 않는다.

# --- 선택: 뉴스 실제 수집 ---
requests>=2.32

# --- HTTP 서버 (server.py) ---
# 자바 앱이 부르는 창구. 라이브러리만 쓸 때는 필요 없다.
fastapi>=0.115
uvicorn>=0.32
