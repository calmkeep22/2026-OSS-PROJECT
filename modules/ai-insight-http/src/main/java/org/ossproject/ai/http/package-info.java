/**
 * AI 분석 서비스를 HTTP 로 부른다.
 *
 * <p>분석 엔진은 파이썬이라 같은 프로세스에서 부를 수 없다. 호출마다 프로세스를 새로
 * 띄우면 준비에만 10초가 들어 쓸 수 없으므로, 한 번 띄워 둔 서버에 붙는다.
 *
 * <p>서버가 없거나 준비되지 않았으면 값을 지어내지 않고 그 사실을 알린다.
 */
package org.ossproject.ai.http;
