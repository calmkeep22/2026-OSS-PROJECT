/**
 * 음성과 소리 출력을 위한 계약.
 *
 * <p>어떤 합성 엔진을 쓰는지는 여기서 정하지 않는다. 화면과 알림 계층은 이 계약만 보고,
 * 실제 구현은 {@link org.ossproject.accessibility.infrastructure} 가 운영체제별로 고른다.
 */
package org.ossproject.accessibility.port;
